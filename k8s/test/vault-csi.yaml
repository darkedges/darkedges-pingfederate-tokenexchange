apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: vault-database
spec:
  provider: vault
  secretObjects:
  - data:
    - key: password
      objectName: db-password
    secretName: dbpass
    type: Opaque
  parameters:
    vaultAddress: "http://vault.hashicorp-vault:8200"
    roleName: "secret"
    objects: |
      - objectName: "db-password"
        secretPath: "secret/data/db-pass"
        secretKey: "password"
---
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: vault-spiffe-provider
spec:
  provider: vault
  parameters:
    # The Vault role authorized to issue these SPIFFE IDs
    roleName: "spiffe" 
    vaultAddress: "http://vault.hashicorp-vault:8200"
    objects: |
      - objectName: "spiffe-cert"
        secretPath: "spiffe_intermediate/issue/spiffe" # Example path for certs
        secretArgs:
          common_name: "webapp.darkedges"
          ttl: "5m"
      - objectName: "spiffe-jwt"
        secretPath: "identity/oidc/token/application_identity" # Example path for JWTs
---
apiVersion: v1        
kind: ServiceAccount
metadata:
  name: webapp-sa
  namespace: sandbox
---
kind: Pod
apiVersion: v1
metadata:
  name: webapp
spec:
  serviceAccountName: webapp-sa
  containers:
  - image: jweissig/app:0.0.1
    name: webapp
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
    env:
    - name: DB_PASSWORD
      valueFrom:
        secretKeyRef:
          name: dbpass
          key: password
    volumeMounts:
    - name: secrets-store-inline
      mountPath: "/mnt/secrets-store"
      readOnly: true
    - name: spiffe-volume
      mountPath: "/var/run/secrets/spiffe"
      readOnly: true
  volumes:
    - name: secrets-store-inline
      csi:
        driver: secrets-store.csi.k8s.io
        readOnly: true
        volumeAttributes:
          secretProviderClass: "vault-database"
    - name: spiffe-volume
      csi:
        driver: secrets-store.csi.k8s.io
        readOnly: true
        volumeAttributes:
          secretProviderClass: "vault-spiffe-provider"